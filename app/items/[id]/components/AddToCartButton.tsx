'use client';

import { Button } from '@nextui-org/react';

function AddToCartButton() {
  return (
    <div>
      <Button className='mt-4 font-bold w-full' color='primary'>
        Add to Cart
      </Button>
    </div>
  );
}

export default AddToCartButton;
